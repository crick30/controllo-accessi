# Ambiente di test/integrazione
# Copia o rinomina in .env oppure imposta APP_ENV_FILE=.env.test
# per caricare automaticamente questi valori.

APP_ENV=test
APP_SIMULATE_ROLE=operator

# Gruppi di esempio per simulare membership AD nei test
USER_GROUPS=CN=AccessOperators,OU=Security,DC=example,DC=com;CN=AccessAdmins,OU=Security,DC=example,DC=com

APP_THEME=auto
APP_LIGHT_START=7
APP_LIGHT_END=19

# Identità usata per tracciare le azioni nei log di audit
APP_USER=test_runner
