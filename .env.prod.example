# Ambiente di produzione (senza ruoli simulati)
# Copia o rinomina in .env oppure imposta APP_ENV_FILE=.env.prod
# e valorizza le variabili con i dati reali dell'infrastruttura.

APP_ENV=production
# Lascia vuoto APP_SIMULATE_ROLE in produzione per non bypassare i controlli.
APP_SIMULATE_ROLE=

# Imposta i gruppi effettivi dell'utente autenticato (se usati via reverse proxy/SSO)
USER_GROUPS=CN=AccessOperators,OU=Security,DC=example,DC=com;CN=AccessAdmins,OU=Security,DC=example,DC=com

APP_THEME=auto
APP_LIGHT_START=7
APP_LIGHT_END=19

# Nome utente effettivo per i log di audit (es. header SSO)
APP_USER=
